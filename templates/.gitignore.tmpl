# Runtime state
next_instruction.md
round_counter
session_id
last_run_name
train.pid
current.log
events.jsonl
consecutive_failures
train_completed
HALTED
training_report.md

# Logs (large JSONL files)
logs/

# Claude config
.claude/

# Per-project secrets (agent mode — agent.env may contain API keys)
agent.env
.env
secrets.env

# Python
__pycache__/
*.pyc

# OS
.DS_Store
